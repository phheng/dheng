
 

架构师（JiaGouX）
我们都是架构师！
架构未来，你来不来？




昨天发出的《模型越来越强，为什么大家却开始重写 Harness》里，我们主要在回答"为什么"。发出之后，评论区里一个很集中的反馈是："听起来有道理，但还是没搞清楚 Harness 到底长什么样。"

这个反馈其实很准确。

因为昨天我们回答的是"为什么"，今天更该回答的是"到底是什么"。

而这恰恰是 Harness 最容易出问题的地方。

有人把它理解成工具系统。有人把它理解成 Prompt 外挂。有人把它理解成多 Agent 编排。也有人把它理解成 Sandbox、Hooks、Skills、Memory 这些东西的总和。

这些说法都不能算错。但如果只停在这里，Harness 还是会像一个什么都能往里装的筐。读者听完一圈，最后只会留下一个模糊印象：这好像是"模型外面的很多东西"。


如果一个概念什么都能装，它通常也就什么都没讲清。

本文想做的事情只有一件：把 Harness 从一个容易越讲越糊的热词，重新拆回一个有边界、有层次的工程对象。

为了做到这件事，我最想放在一起读的，是两篇文章。

一篇是 OpenAI 的《工程技术：在智能体优先的世界中利用 Codex》。一篇是 Anthropic 的《Harness design for long-running application development》。

前者讲的是：当代码几乎都由 Agent 生成时，仓库、架构、文档和反馈系统应该怎么被重新设计。 

后者讲的是：当任务一跑就是几小时，模型为什么会跑偏、为什么会自评失灵，以及怎样通过 Planner、Generator、Evaluator 让系统继续收敛。

表面看，一篇更像在讲仓库工程，一篇更像在讲长任务编排。

但放在一起看，它们刚好拼出了同一个东西的不同切面。

本文就从 OpenAI 和 Anthropic 这两篇原文出发，把 Harness 的内部结构拆开看。

如果把我们这段时间写过的几篇推文连起来看，其实已经在不同角度碰到过 Harness 的轮廓了。写《Skills 详解》时，碰到的是知识怎么沉淀进文件系统；写《跟Cloudflare大佬学用 Claude Code》时，碰到的是流程怎么进入执行链路；写《深度拆解 Clawdbot（OpenClaw）架构与实现》时，碰到的是权限、回放和运行时控制。本文更像是把这些分散的线索，重新收成一个结构。

太长不看版
• Harness 之所以容易越讲越糊，不是因为它空，而是因为它本来就是一个上位系统问题。把它拆成知识层、约束与流程层、反馈与运行时层三层看，结构才会清楚。
• 同一个模型，不改 Prompt，不换数据，只换 Harness：单 Agent 20 分钟花 9 美元产出不能用，完整 Harness 6 小时花 200 美元交付了真正可用的应用。差距不在模型，在系统。
• OpenAI 在文中真正补的是知识层和约束层：仓库成为唯一知识源、AGENTS.md 当地图而非百科、架构边界靠 linter 机械执行。
• Anthropic 在文中真正补的是流程层和反馈层：Planner / Generator / Evaluator 分离职责、context reset 对抗长任务失真、外部评估替代自评。
• Harness 不是谁事先设计出来的，往往是在和 AI 反复较劲中慢慢长出来的。而且它正在从隐藏在脚本里的经验，走向可读、可移植、可执行的文件。
• Prompt 管你怎么说，Context 管模型看到什么，Harness 管整个系统怎么运行。三者是包含关系，不是替代关系。
先从一组对比数据说起
在讲结构之前，先说一组数据。

Anthropic 在文中给了一个实验，我觉得比很多定义都更能说明 Harness 到底在干什么。

他们用同一句 Prompt——"做一个 2D 复古游戏编辑器"——让同一个模型跑了两次。

第一次，单 Agent，没有任何外部辅助。跑了 20 分钟，花了 9 美元。打开之后，界面有了，编辑器有了，看上去功能都做了。但真正点进去，核心游戏功能是坏的。角色放上去了，键盘没反应。代码层面，实体定义和运行时之间的接线根本没接通。看起来像是完成了，其实什么都没完成。

第二次，换了完整的 Harness——规划器展开需求、生成器按 Sprint 逐步实现、评估器用 Playwright 跑页面做 QA。同一个模型跑了 6 个小时，花了 200 美元。产出多了不止一个量级：精灵动画、行为模板、音效系统、AI 辅助关卡生成、游戏导出和分享链接，全部做出来了。最关键的是，你真的能控制角色跑和跳。

成本高了 20 多倍。但第一次那份产出，严格说不算产出，因为核心功能是废的。

模型没换，Prompt 没换。变的只是它跑在什么系统里。

这也是我为什么觉得，讲 Harness 不该从定义开始，而该从真实的差距开始。定义是死的，差距是活的。差距会告诉你，这层系统到底在管什么。

Harness 至少在管四类事情
如果把上面那组对比往下拆，你会发现差距不是来自某一个单点。

单 Agent 和完整 Harness 之间，至少差了四类东西：

• 给模型看什么。 单 Agent 只有一句 Prompt。完整 Harness 里，规划器会先把一句话需求展开成 16 个 feature 的产品规格，每个 Sprint 还有专门的 contract 文档——生成器和评估器先谈好"做到什么算完成"，再动手。
• 允许模型做什么。 单 Agent 自己决定所有事。完整 Harness 里，职责被拆开了——规划、实现、验收三件事不该混在同一个脑子里。
• 怎么判断它做得对不对。 单 Agent 自己评价自己。Anthropic 在文中的一个关键观察是：模型在评估自己产出的工作时，往往会自信地给出偏正面的评价，即使在人类看来质量只是一般。所以完整 Harness 里，评估器是独立的，它用 Playwright 真正去点页面、跑功能、看结果。
• 出错之后怎么修。 单 Agent 犯了错可能完全意识不到。完整 Harness 里，Sprint 不通过就打回去，反馈信号具体到"这个函数存在但没被正确触发"。
讲到这里，Harness 就不再像一个抽象概念了。

它更像一组控制问题的总称：模型该看到什么、允许做什么、做完之后谁来验、跑偏了怎么纠正。

拆开看：Harness 内部至少有三层
如果今天要把 Harness 讲透，不能只给定义，要拆结构。

把 OpenAI 与 Anthropic 这两篇原文放在一起看，我觉得最清楚的拆法，不是按公司拆，也不是按组件拆，而是按它到底在解决什么问题来拆。

第一层：知识层
这一层解决的是：模型该读什么、团队经验放哪里、哪些规则必须显式可见。

OpenAI 在文中对这一层的描述很完整。

他们最早踩的一个坑就很有代表性：试过写一份巨大的 AGENTS.md，把所有规则、约定、指引全塞进去。结果很快撞了墙——

• 上下文是稀缺资源。大文件会挤掉真正相关的信息。
• 所有东西都标成"重点"，最后等于没有重点。
• 巨石文档很容易腐烂，而且没法做 freshness、owner、cross-link 这些机械检查。
所以他们后来把 AGENTS.md 的角色改了。它不再是百科全书，而更像一个内容目录——大约 100 行，只告诉 Agent 去哪里找什么。

真正的知识库，被拆进结构化的 docs/ 目录里。设计文档、产品规范、执行计划、技术债追踪、生成物说明，全部版本化地存在仓库中。代码仓库本身变成了记录系统。

AGENTS.md              ← 短，像地图
ARCHITECTURE.md        ← 分层和依赖方向
docs/
├── design-docs/       ← 带验证状态的设计文档
├── exec-plans/        ← 执行计划（活跃 / 已完成）
├── product-specs/     ← 产品规格
├── references/        ← 第三方库的 LLM 可读参考
├── DESIGN.md
├── FRONTEND.md
├── QUALITY_SCORE.md
└── SECURITY.md
这背后有一个判断我觉得很值得带走：

对 Agent 来说，看不见的知识就等于不存在。

Slack 里的讨论是知识。Google Docs 里的共识也是知识。某个资深工程师脑子里的隐性判断也是知识。但只要它没被编码进仓库，对 Agent 来说就像从未发生过。

图片

对 Agent 来说，看不见的知识就等于不存在
所以知识层真正要解决的，不是"文档越多越好"，而是：

把原本散落在人脑和聊天记录里的隐性知识，变成版本化、可引用、可检查的显性工件。

OpenAI 甚至专门跑一个定期运行的"doc-gardening"智能体，扫描过时文档，自动发起修复用的 PR。知识层一旦腐烂，Agent 的决策质量就会连带下滑。

这和我们前面写《Skills 详解》时其实是一条线。Skill 真正有价值的地方，也不是多了一份 Prompt，而是把原本靠聊天临场发挥的方法论，搬进文件系统、版本控制和可复用工件里。

换个角度看，这一层还有一个很实用的判断：别把确定性逻辑全塞进上下文。 凡是可以通过 Hooks、代码规则、工具权限或仓库内工件表达的内容，尽量交给外部系统处理。上下文更适合承载当前任务真正需要推理的那部分信息。

这一层不补，模型永远只能靠临场发挥。

第二层：约束与流程层
这一层解决的是：任务怎么拆、哪些步骤必须先发生、哪个角色负责什么、什么情况下该切换阶段。

Anthropic 在文中对这一层的处理最有代表性。

他们的做法是把系统拆成了 Planner、Generator、Evaluator 三个角色。

Planner 负责把一句模糊需求扩成详细规格——不是乱展开，而是刻意只聚焦产品上下文和高层设计，不去规定细粒度的技术细节。因为他们发现，如果规划器一开始就在实现细节上犯了错，错误会级联传导到下游所有实现中。

Generator 负责真正实现，按 Sprint 逐个做 feature。每个 Sprint 开始前，Generator 和 Evaluator 要先谈好一份"Sprint 契约"——做到什么算完成，用什么标准验收。这份契约不是拍脑袋的，单个 Sprint 就有 27 条验收标准。

Evaluator 负责验收。它不是读代码打分——而是真正去跑。

这个结构表面上像多 Agent 编排。但我更在意的是它背后的工程含义：

原本混在一个模型里的几种职责，被拆成了不同的流程角色。这种拆分不是为了"并行更快"，而是为了"别一边生成一边自我表扬"。

另外，Anthropic 在文中还补了一个很关键的点：context reset。

他们发现，长任务里光靠 compaction（原地压缩历史）还不够。有些模型会随着上下文越来越满，开始出现一种"上下文焦虑"——提前收尾，或者逐渐失去一致性。

所以在旧版本 harness 里，他们会定期清空上下文窗口，重新启一个新会话，再通过结构化工件把状态交接给下一个 Agent。

这个设计听上去笨，但很工程。它说明流程层真正要管的，不只是任务顺序。还包括：会话何时切换、状态如何交接、哪些信息必须进入 handoff artifact。

如果把这层和我们前面写《跟Cloudflare大佬学用 Claude Code》时提到的那套 Research -> Plan -> Implement 工作流放在一起看，会更容易理解：流程层真正要做的，不是把步骤写满，而是让架构决策、阶段切换和完成标准进入执行链路。

OpenAI 那边的约束层走的是另一条路——不拆角色，而是把约束编码成机械规则。

他们把业务域依赖方向直接收紧成固定层级：Types → Config → Repo → Service → Runtime → UI。每一层只能"向前"依赖，横切关注点通过一个单一的 Providers 接口进入。违反架构约束的 PR 会被 linter 和结构测试自动拦截。


图片
OpenAI 在文中给出的分层领域架构图
对人类团队来说，这种约束有时会显得有点重。但对 Agent 来说，它反而像一条很清楚的护栏。

OpenAI 在文中有一句话很到位：在以人为本的工作流中，这些规则可能会让人感到迂腐或束缚。有了智能体，它们就成了倍增器。

第三层：反馈与运行时层
这一层解决的是：模型做完之后，谁来告诉它对不对。

这是 Harness 里最容易被低估的一层。

很多人对 Harness 的理解停在"给模型配上工具"。但工具本身不是反馈。工具只是让模型有了手。反馈是让它有了感官。

Anthropic 在文中让我印象最深的地方，就在这里。

他们发现，真正拉开差距的，不只是让模型多做一轮，而是给它一个足够挑剔、能接触真实环境的 Evaluator。

这个 Evaluator 不是读代码打分。它会用 Playwright 真正去跑页面、点按钮、看行为、发现没接上的地方，再把这些问题作为反馈交给 Generator。

原文里列了几个 Evaluator 实际发现的问题，我觉得很能说明这层反馈的颗粒度：

• 矩形填充工具应该能拖拽填满一个区域，但实际只在起点和终点放了瓷砖，fillRectangle 函数存在但没被正确触发。
• 删除实体需要同时设置 selection 和 selectedEntityId，但点击实体只设置了后者，条件判断逻辑写错了。
• PUT /frames/reorder 路由定义在 /{frame_id} 路由之后，FastAPI 把 reorder 当成整数解析，返回 422 错误。
这些不是"看代码打分"能发现的问题。它们只有在真正跑起来、点进去之后才会暴露。

而且调教 Evaluator 本身也不容易。Anthropic 的工程师写道：开箱即用的 Claude 是一个很差的 QA Agent。在早期的运行里，他看着 Evaluator 发现了真正的问题，然后自己说服自己这不是大事，然后通过了验收。花了好几轮迭代——读日志、找评估器判断偏差、更新提示——才把评估质量调到合理水平。

OpenAI 在文中对反馈层的补法不太一样，但方向相同。

他们做的事情包括：

• 让应用能按 git worktree 启动，每次 Codex 改动都能启动一个独立实例去验证
• 把 Chrome DevTools 协议接入 Agent 运行时，让它能截图、查 DOM 快照、驱动页面导航
• 把日志、指标、追踪做成本地可观测性堆栈，Agent 可以直接用 LogQL 查日志、PromQL 查指标

图片
Codex 通过浏览器验证自己的修改是否真的生效

图片
可观测性堆栈进入 Agent 的反馈闭环
也就是说，他们做的不是"再多给点说明"，而是：

把应用的可观察性和可验证性直接接进 Agent 的工作回路。

有了这层之后，像"确保服务启动在 800ms 内完成""这四个关键用户旅程中的任何跨度都不得超过两秒"这样的要求才变得可执行。

他们提到，经常看到单次 Codex 运行持续工作超过六个小时——通常是在人类睡觉的时候。没有这层反馈闭环，这种长时间自主运行是不可能做到的。

这和我们前面拆 OpenClaw 时反复遇到的那些问题也是同一类：权限怎么控、状态怎么回放、浏览器语义快照怎么接回系统。看起来像不同产品的不同实现，底层其实都在补运行时和反馈层。

Harness 不是设计出来的，是长出来的
很多人看完 OpenAI 和 Anthropic 的文章之后，第一反应可能是：这套东西好复杂，得先设计好整个系统架构，再开始跑 Agent。

但真实的成长路径，往往不是这样的。

Mitchell Hashimoto 在今年 2 月写过一篇文章。他把自己使用 AI 的进化分成六个阶段，其中第五阶段叫 Engineer the Harness。他的方法极其朴素：每次 Agent 犯一个错，就把修复方式写进配置文件，让它下次别再犯。他拿自己的终端模拟器 Ghostty 举例：

那个文件里的每一行都基于一次 Agent 的不良行为，而且几乎完全解决了这些问题。

很多一线实践也很像。最开始只是一个简单的规则文件，后来慢慢加上路由、Hooks、Skills、知识库和权限边界。没有人一上来就把它设计完整，它是在一次次真实问题里长出来的。

这件事很重要。

因为它意味着 Harness 的起点不是"画一张完整架构图"，而是"从第一个犯错开始补"。

很多团队所谓的 Harness，不是一开始就完整存在的。它是在"模型犯错 → 人类补规则 → 系统下次少犯"这个循环里慢慢形成的。

而最近有一篇清华和哈工大的论文，则往前又走了一步。

他们提出了一种叫 NLAH（Natural-Language Agent Harness）的东西。核心思路是：能不能把 Harness 里的控制逻辑，抽出来写成一种更接近自然语言、但仍然可执行的文件？

里面包括角色边界（谁负责什么）、契约（什么情况下调 tool，什么情况下转人工）、状态语义（中间结果存在哪，怎么命名）、失败分类（出错了怎么处理）。配套一个 IHR（Intelligent Harness Runtime），把文件里的逻辑真正跑起来。

这说明 Harness 正在走出"只能藏在代码和脚本里"的阶段。它开始被显式语言化、文件化。

不管这条路最终能走多远，它背后的方向值得注意：

Harness 的核心不是某个具体框架，而是控制逻辑能不能被表达、复用、迁移和执行。

Harness 会不断变化，但底层问题不会消失
写到这里，需要补一个边界。

因为只要今天开始拆结构，读者很容易误会成"那是不是把规则、工具、记忆、Agent、评估器越堆越多越好？"

Anthropic 在文中有个很重要的提醒：Harness 的每个组件，本质上都编码了对模型局限性的一个假设。模型变强之后，有些假设不再成立。

一个很具体的例子：他们旧版本 Harness 用的是 Sonnet 4.5，这个模型有很强的"上下文焦虑"，必须靠 context reset 来对抗。到了 Opus 4.5，这个问题基本消失了，sprint 分解也可以去掉，Harness 反而变得更轻了。

但评估器去不掉。

因为不管模型变得多强，"自己评价自己的工作"这个能力的提升速度，远远跟不上生成能力的提升。

Anthropic 的工程师用了一句很精准的话来描述这个判断：

评估器的价值取决于任务是否处于当前模型独立可靠完成的边界之外。这个边界会随模型能力移动，但边界本身不会消失。

所以更现实的做法是：

一边观察模型新的能力边界，一边拆掉那些不再承重的组件，一边在新的边界处补上新的约束和反馈。

Harness 的空间不会随着模型变强而消失。它只会移动。

Prompt、Context、Harness：不是替代，是包含
最后补一层关系。

最近这两天的材料里，"从 Prompt 到 Context 到 Harness"这条演进线被讲了很多次。但这条线很容易被误读成"新词替代旧词"。

更稳的理解是：每一层都包含前一层，但每一层解决的问题不一样。

Prompt 关心的是：你怎么说。措辞、格式、few-shot、Chain of Thought。核心是优化"一次性的输入"。

Context 关心的是：模型到底看到了什么。RAG 检索、长上下文管理、工具定义、动态信息注入。核心是设计模型的"输入空间"。

Harness 关心的是：整个系统怎么运行。知识怎么挂进去、约束怎么生效、反馈怎么接回来、经验怎么留下来、任务在多个会话之间怎么续跑。核心是设计 Agent 的"运行时环境"。

这里面真正的变化不是词汇升级，而是问题升级。

在短对话时代，把 Prompt 写好就够了。任务开始变长以后，Context 变成了瓶颈。而一旦模型开始读仓库、改代码、跑测试、调浏览器、持续工作几个小时，你会发现只优化"说法"和"输入"，已经远远不够了。

如果把任务按"目标清晰度"和"验证自动化程度"来分，目标明确、结果可自动验证的那一类，通常最适合 Agent 发挥。而 Harness 真正要做的，往往就是把更多任务往这个区域推进。

不是让模型更聪明，而是让系统把"对错"变成机器可以执行的判断标准。

图片

写在最后
本文真正想做的事情其实很简单。

就是把 Harness 从一个容易越讲越糊的热词，重新拆回一个有层次的工程对象。

三层：知识层、约束与流程层、反馈与运行时层。每一层解决的问题不一样，补法也不一样。

OpenAI 在文中提醒我们：当 Agent 成为主要生产力，仓库就是大脑，架构边界就是护栏，文档和规则要版本化进仓库，而不是散在聊天记录和脑子里。

Anthropic 在文中提醒我们：模型不擅长评价自己的工作，职责分离比功能堆叠更重要，长任务需要结构化的续跑机制，而 Harness 的组件应该随着模型迭代不断减重和调整。

Mitchell Hashimoto 和很多一线实践共同说明了一件事：Harness 不是一个你拿来套用的框架，而是一段会慢慢积累出来的经验——从第一个犯错开始补，从第一条规则开始写。

如果说 Prompt 是在教模型怎么回答，Context 是在决定模型看见什么，那么 Harness 真正关心的，其实是更后一层的事：

怎么让一个会生成的模型，慢慢变成一个能在真实系统里稳定交付结果的工作体。

参考材料
• OpenAI Codex 团队,《Harness Engineering》
https://openai.com/zh-Hans-CN/index/harness-engineering/
• Anthropic,《Harness design for long-running application development》
https://www.anthropic.com/engineering/harness-design-long-running-apps
• Mitchell Hashimoto,《My AI Adoption Journey》, mitchellh.com, 2026 年 2 月
• 清华大学 + 哈工大,《Natural-Language Agent Harnesses》, 2026 年 3 月


如喜欢本文，请点击右上角，把文章分享到朋友圈
如有想了解学习的技术点，请留言给若飞安排分享



因公众号更改推送规则，请点“在看”并加“星标”第一时间获取精彩技术分享

·END·

相关阅读：

模型越来越强，为什么大家却开始重写 Harness

如何让单个 Agent 做长任务不失真：Anthropic 给出了一套更工程化的答案

Claude Code高手的 8 个 Claude Code 实战习惯

别从 README 开始：一个架构师会怎样翻 Codex 仓库

Spec 不是代码的替代品，它是 AI Coding 的上下文管理层

如何让 Agents 自己设计、升级 Agents

OpenAI怎么把开源项目维护做成工作流：Skills、AGENTS.md 和 CI 的一套组合拳

Claude Skills 入门：把“会用 AI”变成“可复制的工程能力”

一套可复制的 Claude Code 配置方案：CLAUDE.md、Rules、Commands、Hooks

Claude Code 最佳实践：把上下文变成生产力（团队可落地版）
把 AI 当成新同事：Agent Coding 的上下文与验证体系
一周写百万行的背后：Cursor长时间运行 Agent 的工程方法论
2026年生活重启指南
我真不敢相信，AI 先加速的是工程师。
扒一扒 Claude Cowork 系统提示词：Anthropic 如何打造数字同事
Cowork 安全架构深度解析：从 Claude Code 到 Cowork，Anthropic 如何把“可控”做成产品
Anthropic官方万字长文：AI Agent评估的系统化方法论
银弹还是枷锁？Claude Agent SDK 的架构真相
Claude Code创始人亲授13条使用技巧
Claude Code 内部工具开源 code-simplifier：终结 AI 屎山代码的终极方案
版权申明：内容来源网络，仅供学习研究，版权归原创者所有。如有侵权烦请告知，我们会立即删除并表示歉意。谢谢!

架构师
我们都是架构师！



图片

关注架构师(JiaGouX)，添加“星标”

获取每天技术干货，一起成为牛逼架构师

技术群请加若飞：1321113940 进架构师群

投稿、合作、版权等邮箱：admin@137x.com




暂无评论
