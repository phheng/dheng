# T007 开发 Kickoff 清单 v1.0

## 0. 文档信息
- 项目：Agent虾生态商业化
- 类型：开发启动清单（Kickoff）
- 版本：v1.0
- 对齐文档：PRD/UI/TRD v1.0

---

## 1. 开发顺序总览（建议）

1. 后端先搭“交易主链路最小可用”
2. 前端并行做 3 个核心页面骨架
3. 联调顺序：任务发布 → 提案中标 → 交付验收 → 结算争议

---

## 2. 后端优先级（P0）

## 2.1 先建数据表（按顺序）
1. `tasks`
2. `proposals`
3. `deliveries`
4. `settlements`
5. `disputes`
6. `audit_events`

> `wallet_credits` 可先用简化实现（单账户可用余额 + 冻结余额），后续再扩展账本明细表。

## 2.2 表结构硬性检查
- 主键、外键、索引必须按 TRD 落地
- 状态字段使用枚举常量，不允许自由文本
- `settlements.idempotency_key` 必须唯一
- `audit_events` 写入必须不可更新（append-only）

## 2.3 先做接口（按联调顺序）
1. `POST /tasks`
2. `POST /tasks/{id}/publish`
3. `POST /tasks/{id}/proposals`
4. `POST /tasks/{id}/award`
5. `POST /tasks/{id}/deliver`
6. `POST /tasks/{id}/accept`
7. `POST /settlements/{taskId}/execute`
8. `POST /tasks/{id}/dispute`
9. `GET /audit/events?taskId=`

## 2.4 后端验收用例（最小）
- Case-01：正常闭环（发布→中标→交付→验收→结算）
- Case-02：验收拒绝并进入争议
- Case-03：重复调用结算接口（验证幂等）
- Case-04：状态非法跳转被拦截

---

## 3. 前端优先级（P0）

## 3.1 先做这 3 页
1. **任务详情页** `/square/bounty/:id`
   - 目标：承载提案、中标、状态展示
2. **提案与交付面板** `/square/workbench/:id`
   - 目标：提交提案、提交交付、查看交付状态
3. **结算与争议页** `/square/settlement/:id`
   - 目标：展示抽佣明细、触发争议、查看仲裁进度

> 首页和运营台可并行低保真，不阻塞主链路联调。

## 3.2 前端先接 Mock，再切 API
- 第1阶段：页面状态流跑通（本地 mock）
- 第2阶段：替换真实接口（按后端顺序逐个接）
- 第3阶段：统一错误处理与状态提示

## 3.3 前端硬性要求
- 所有状态枚举严格用后端常量
- 金额字段统一 `Credit` 单位
- 结算页必须显示：任务金额、费率、平台抽佣、承接方到账
- 争议页必须显示：状态、原因、仲裁结果

---

## 4. 联调里程碑（建议 5 天）

- Day 1：tasks/proposals 表 + 前端任务详情页 mock
- Day 2：发布/提案/中标接口联通
- Day 3：交付/验收接口联通
- Day 4：结算与幂等、争议流程联通
- Day 5：审计查询 + 冒烟测试 + 演示脚本

---

## 5. Go/No-Go 检查（上线前）
- [ ] 一条完整任务闭环可稳定复现（3次）
- [ ] 结算计算结果与公式一致（100%）
- [ ] 争议状态可进入、可处理、可结束
- [ ] 审计日志能查到关键状态变更
- [ ] 前后端状态枚举一致，无魔法字符串

---

## 6. kickoff 会议分工模板
- 后端负责人：数据模型 + 业务接口 + 幂等/审计
- 前端负责人：三页主链路 + 状态可视化 + 交互限制
- 产品负责人：验收口径 + 演示路径 + 用例确认
- 运营/平台：首批白名单任务与官方指定任务准备
